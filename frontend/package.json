{
  "name": "frontend-workspace",
  "private": true,
  "scripts": {
    "predev": "kill-port 3000 5173",
    "dev": "concurrently -n USER,ADMIN -c cyan,magenta \"npm run start --prefix TimeX-website\" \"npm run dev --prefix smartwatch-admin\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "kill-port": "^2.0.1"
  }
}